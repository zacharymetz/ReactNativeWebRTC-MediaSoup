import { Response } from 'express';
import { Controller, Req, Body, Post, UseBefore, HttpCode, Res } from 'routing-controllers';

@Controller()
export class DebugController {
    /** 2 routes, one that has a web app to view the 
     * resulting mobile streams to verify they are working 
     * one route for the html and one for the css
     */
   
}